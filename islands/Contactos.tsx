import { FunctionComponent } from "preact";
import { useState } from "preact/hooks"
import { AgendaResponse } from "../types.ts"
/*
const AgendaCSR: FunctionComponent = () => {

    const fetchContactos = async () => {
        const response = await fetch ("/agendacsr")

        const data = response
    }

    return (Response)

}
*/
